#include<stdio.h>
int main(){
int n,m,i; 
int sum=0; 
printf("Enter a number");  
scanf("%d",&n);      

for(i=1;i<n;i++){
    m=n%i;  
     if(m==0){ 
        sum=sum+i;  
        }  
     }  
if (sum==n){ 
    printf("perfect");  
}
else{
    printf("not perfect"); 
     }  
}
